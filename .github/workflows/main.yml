name: dotnet Package

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        dotnet-version: ["6.0.x"]


    steps:
      - uses: actions/checkout@v3
      - name: Setup Dotnet Content
        uses: actions/checkout@v3
        with:
          dotnet-version: "6.0.x"
      - name: Install Deps
        run: dotnet restore
      
